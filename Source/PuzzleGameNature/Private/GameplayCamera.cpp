// Fill out your copyright notice in the Description page of Project Settings.


#include "GameplayCamera.h"


//Constructor
AGameplayCamera::AGameplayCamera()
{

	
}


//Calls whenever the game starts or this actor spawns
//void AGameplayCamera::BeginPlay()
//{
	//It does not work as expected, and I spent hours already. Putting it in comments

	/*
	TArray <AActor*> TempListOfRails;
	if (CameraRailPtr != nullptr)
	{
		CameraRailPtr->GetAllChildActors(TempListOfRails, true);
		//foreach loop
		for (AActor* i : TempListOfRails)
		{
			RailActorPtr = i;
		}
	}

	if (RailActorPtr != nullptr)
	{	//Attaches Camera to rail
		AttachToActor(RailActorPtr, FAttachmentTransformRules::KeepRelativeTransform);
	}
	*/
//}
